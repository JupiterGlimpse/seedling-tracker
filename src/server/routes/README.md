# routes - API 路由层

> ⚠️ 一旦我所属的文件夹有所变化，请更新我。

## 架构说明
定义所有 HTTP API 端点，负责参数验证和响应格式化。
调用 services 层处理业务逻辑，返回 JSON 响应。

## 文件列表

| 名称 | 地位 | 功能 |
|------|------|------|
| `progress.js` | 路由 | 处理进展记录相关请求（创建、查询） |
| `reminder.js` | 路由 | 处理提醒相关请求（获取、更新状态） |
